<h2>Create Book </h2>

<%= form_for @book do |f| %>
  <div>
    <%= f.label :title %>
    <%= f.text_field :title %>
 
    <%= f.label :author %>
    <%= f.text_field :author %>
  </div>

  <br>
  <br>

  <%= f.label :description %><br>
  <%= f.text_area :description %>

  <br>
  <br>

  <%= f.label :genre_id %>
  <%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "Select Genre" %>

  <br>
  <br>

  <%= f.fields_for :genre, @book.build_genre do |genre_field| %>
    <%= genre_field.label :name %>
    <%= genre_field.text_field :name %>
  <% end %>

  <div>
    <%= f.submit %>
  </div>

<% end %>